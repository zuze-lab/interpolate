import to from './to';
import { default as from } from './from';
export { getter, setter } from './utils';
const interpolate = from;
const unterpolate = to;

export { to, from, interpolate, unterpolate };
